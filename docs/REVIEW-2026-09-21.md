> **Historical record, superseded where inconsistent:** see [Pages review](REVIEW-PAGES-2026-09-20.md), [current method](METHODOLOGY.md), and [source audit](SOURCES.md). Earlier exact-date/fully-verified claims are not current assurances.

# Session 2 re-verification — 2026-09-21 (this pass)

Session 1's record follows below. Session 2 performed a fresh live re-verification
of every time-sensitive input (page fetches; the sandbox's direct TLS to
statsapi.mlb.com / site.api.espn.com remains blocked, as documented in
[LIMITATIONS-NEXT.md](LIMITATIONS-NEXT.md)) and made the corrections below.

## Pass 1: live re-verification, line by line

- **Westwood One NFL schedule** — all four page chunks re-fetched and every row of
  `data/verified/nfl_westwoodone_2026.json` compared against the live page:
  **71/71 match** (matchup, date, ET air time, feed, venue, TBA slots, event IDs).
  The file's own meta miscounted the rows as "70"; corrected to 71 and a
  session-2 re-verification note added. No row added or changed since
  2026-09-20; the page's "Upcoming" tab now starts at 09-21 (NYG@LAR 7pm ET), the
  09-19/09-20 rows having moved to its "Past" tab.
- **49ers full schedule (49ers.com)** — all 3 preseason results and all 16
  regular-season kickoffs re-read and identical to the stored rows (Week 3
  09-27 1:05 PM PDT vs ARI; Week 4 10-04 1:25 PM vs DEN; Week 11 11-22 5:20 PM PST
  Mexico City; Week 17 01-03 5:20 PM vs PHI; Week 18 still date/time TBD).
  **New finding:** the club lists per-game radio stations — Weeks 2-3 on
  KSFO 810 AM / KSAN 107.7 FM, Week 4 onward on KSAN 107.7 FM / KNBR 104.5 FM /
  680 AM, none listed for the Melbourne opener or preseason. The builder had
  flattened every game into one blanket station label; it now stores per-game
  `radio` fields and propagates them, with a regression test. Week 2 final
  (W 35-13) recorded.
- **Athletics home venue (statsapi.mlb.com, teamId=133)** — every A's home game
  through 09-27 is listed at **Sutter Health Park, Sacramento**, consistent with
  the stored KSTE 650 AM (Sacramento) / KNEW 960 AM (Bay Area) radio note. This
  week's A's: vs LAA 09-22/09-23, vs HOU 09-24/09-25/09-26 (all 6:40 PM PT),
  09-27 12:05 PM PT — all fetched live from the API.
- **MLB season frames (statsapi.mlb.com)** — 2026 re-verified live (spring
  02-20 → 03-24, regular 03-25 → 09-27, All-Star 07-14, postseason frame
  09-28 → 10-31); 2027 re-verified live (spring 02-19 → 03-24, full Opening Day
  03-25, All-Star 07-13, regular end 09-26, postseason 09-27 → 10-31). Both
  match `seasons.json` exactly. The 2026-2029 range query still returns 2026
  only, confirming 2028/2029 remain ESTIMATED.
- **MLB 2026 postseason (statsapi.mlb.com)** — a fresh query for 09-28 → 10-31
  returns **53 postseason game records** and every one still carries the
  07:33:00Z sentinel with `startTimeTBD=true` and seed-placeholder teams
  ("AL Wild Card #2" etc.). **No first-pitch time is published as of
  2026-09-21**; the stored "UNRESOLVED" status is now corroborated by today's
  API state, not only by yesterday's snapshot. Wild Card Series begin Tuesday
  2026-09-29 (tracker text).
- **MLB clinch tracker (mlb.com)** — re-read: still exactly **six berths** (Rays
  9/11, Brewers 9/11 + NL Central 9/15, Dodgers 9/14 + NL West 9/17, Yankees
  9/14, Braves 9/18 + NL East 9/20, Red Sox 9/20). No new clinch since session 1;
  the IR-32 correction stands.

## Pass 2: bugs, assumptions, edge cases

- Fixed the WWO meta row count (70 → 71) and recorded session-2 re-verification
  notes in the WWO, 49ers and postseason data files.
- Added the **"Spring Training: included vs excluded" side-by-side table** to the
  All windows tab. Before this pass both readings were only visible via a
  toggle; the user asked to see the no-Spring-Training schedule *and* keep the
  with-Spring-Training one for comparison, so both are now shown at once with
  the delta the strict rule costs. Four regression render checks added.
- Added the 49ers per-game radio regression test so the blanket-label
  flattening cannot return silently.
- Confirmed the day checker's live MLB path is why today's (2026-09-21) Giants
  game (MIN @ SF, 6:45 PM PT, officialDate 2026-09-21 in the API) appears on the
  deployed page although the checked-in offline bundle is a partial snapshot;
  the offline fallback and the MLB season-frame reservation remain covered by
  the existing render checks.

## Pass 3: requirement re-check against the original request

- Free time with/without Spring Training, both visible without a toggle: done
  (new side-by-side table).
- The three sections (MLB + 49ers + WWO; + Stanford/Cal; + Earthquakes) and
  their side-by-side comparison: unchanged, still verbatim definitions, still
  compared across all four years under both interpretations.
- Best 1/2/3-week vacation windows 2026-2029 with verified vs estimated
  status: unchanged; every status label re-checked against today's API state
  (2026 official inputs · modeled gaps; 2027 MLB VERIFIED, rest EST; 2028-29
  ESTIMATED).
- Official verified links: every primary link on the Sources tab was re-fetched
  today (WWO NFL page, 49ers.com schedule, statsapi endpoints, mlb.com clinch
  tracker); redirect behavior is disclosed in SOURCES.md.
- No hallucinations: no new date, time, team or station was invented this pass;
  the only changes are count corrections, per-game radio attribution from the
  club's own listing, and re-verification timestamps.
- Pull request created and merged to `main` with next-session suggestions in
  [LIMITATIONS-NEXT.md](LIMITATIONS-NEXT.md).

## Reproduction

```bash
npm ci
npm run build
npm test
python3 -m unittest discover -s tests
```

---

# Implementation review — 2026-09-21 (session 1)

> Second 2026-09-21 pass (this file's original text is kept below the new
> three-pass record; the pass below supersedes it where they differ).

## Pass 1: implement and verify

- Re-verified the Westwood One NFL schedule **line by line** against the live
  page (70/70 rows match). Re-verified the Westwood One NCAAF grid, the MLB
  clinch tracker, the MLB Stats API year queries (range vs individual), the
  Super Bowl LXIII announcement and the Sharks/Warriors/Valkyries radio
  findings. Every correction is logged in IRREGULARITIES.md (IR-32…IR-38) and
  SOURCES.md ("Re-verified 2026-09-21").
- Fixed the Rays/Braves division error, the seasons headline contradiction, and
  the Super Bowl LXIII status drift across `nfl_calendar.py`, `seasons.json`,
  tests and docs.
- Added the wider live-radio scope: Warriors (NBA) and Valkyries (WNBA) on
  KGMZ 95.7 as high-priority busy time in the `all` scope only, with per-game
  95.7 flags; streaming-only games (Audacy-only Valkyries rows, Sharks) are
  disclosed exclusions that never block an AM/FM listener.
- Added 17 regression tests (`tests/test_other_radio.py`); full suites pass.

## Pass 2: bugs, assumptions, edge cases

- Checked ET→PT conversion on the Warriors grid (evening ET tips land on the
  correct Pacific date; matinees too) and the PT-native Valkyries rows.
- Checked the one-row-per-date invariant on the Warriors list and the +5
  playoff-window invariant on the Valkyries list.
- Checked that no NBA/WNBA game can leak into Sections 1–3 (asserted by test),
  and that `day_report` reads busy under `all` but free under Section 1 for the
  same game (asserted by test).
- Checked the `all`-scope vacation collapse (IR-38) is disclosed in
  VACATION-WINDOWS.md rather than presented as a mysterious regression.
- Checked DST-day and TBD-envelope behavior for the new leagues via the shared
  engine paths (identical code, shared constants).

## Pass 3: requirement re-check against the original request

- Three sections exactly as specified, side-by-side comparison: present and
  unchanged (tests assert section isolation).
- Best vacation windows 2026–2029 with official sources or labelled estimates:
  present; 2027 MLB verified; 2028–2029 MLB and 2027+ NFL estimated with
  `basis` text; Super Bowl LXIII now a verified anchor.
- Day scoreboard with yesterday/tomorrow navigation, month calendar, PST/PDT
  free windows and duration-based estimates: present and covered by render tests.
- MLB postseason TBD resolution from clinch facts: `resolution_progress`
  records exactly what clinching resolves (and that matchups/times remain
  unpublished until ~2026-09-27). No phantom times or matchups.
- Westwood One national games: verified line by line; airtime-vs-kickoff
  handling unchanged and disclosed.
- Other Bay Area live-radio sports as high priority: Warriors/Valkyries added;
  Sharks answered with a sourced exclusion; remaining gaps itemized rather than
  silently ignored.
- Pull request created and merged to `main` with next-session suggestions in
  LIMITATIONS-NEXT.md.

## Reproduction

```bash
npm ci
npm run build
npm test
python3 -m unittest discover -s tests
```

---

# Implementation review — 2026-09-21 (earlier record)

## Pass 1: functional verification

- Confirmed the three requested comparable definitions are represented in `data/verified/profiles.json`:
  1. MLB + 49ers (preseason, regular season, postseason) + Westwood One national NFL.
  2. Section 1 plus Stanford and California football.
  3. Section 2 plus San Jose Earthquakes.
- Confirmed MLB data is represented as all-club schedule data, with Giants and Athletics tagged high priority.
- Confirmed the scoreboard, day navigation, PT time-of-day free windows, vacation-window analysis, radio inventory, source links, and review queue are present.
- Confirmed 2028–2029 unreleased schedules are labelled estimated rather than presented as official.

## Pass 2: bug and edge-case review

- Ran the Python suite: **75 passed** (the two optional skips are environment-dependent live-feed checks).
- Installed the locked Node dependencies with `npm ci` and ran the browser-rendering suite: **52 passed**.
- Checked critical edge cases covered by the suite: MLB postseason games with TBD first pitch, postseason travel days, DST-safe day lengths, overnight intervals, section switching, disclosure of games outside the selected section, and offline MLB fallback.
- Verified that unconfirmed times are envelope-blocked and visibly labelled rather than silently omitted.

## Pass 3: requirement re-check

- Verified the user-facing comparison table reports all four years and all three trip lengths (one, two, and three weeks).
- Verified source and limitation disclosures are available in the Sources, Radio, Needs Review, and documentation views.
- Verified the implementation does not claim that estimated 2027–2029 NFL or 2028–2029 MLB schedules are released schedules.

## Remaining limitations

This review does not turn estimates into official schedules. The application still needs refreshed official inputs when they become available: 2026 postseason first-pitch times and matchups, flexible NFL dates and Westwood One assignments, Stanford/Cal kickoff times, complete Earthquakes fixtures, future MLB/NFL schedules, and postseason/bowl qualification. Basketball and hockey are intentionally outside the three requested sections. These items remain visibly documented in `docs/LIMITATIONS-NEXT.md` and the Needs Review tab.

## Reproduction

```bash
npm ci
npm test
python3 -m unittest discover -s tests
```
